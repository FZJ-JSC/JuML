# Test for DISTANCES
ADD_EXECUTABLE(DISTANCES_TEST Distances.cpp)
TARGET_LINK_LIBRARIES(DISTANCES_TEST core spatial gtest gtest_main ${CMAKE_THREAD_LIBS_INIT} ${AF_LIBS})
ADD_TEST(DISTANCES_TEST DISTANCES_TEST)